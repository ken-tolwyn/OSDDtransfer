[project]
name = "osddtransfer"
version = "0.1.0"
description = "One-way UDP data diode transfer with parity and dedup metadata hooks"
requires-python = ">=3.11"

[tool.pytest.ini_options]
pythonpath = ["src"]
